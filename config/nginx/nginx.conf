# TODO: Integrante 4 - MÓDULO TRANSVERSAL DE SEGURIDAD (SecOps)
# Configurar el proxy inverso (Nginx) para enrutar, validar y proteger las conexiones WebSocket
# de las Estaciones Médicas (puerto 8001, ruta /ws-medicas) y Terminales Administrativas (puerto 8002, ruta /ws-administrativas).
